import Navbar from "@/components/Navbar";

const Header = () => {
  return (
    <header>
      <Navbar />
      <div className="w-full h-[1px] bg-border" />
    </header>
  );
};

export default Header;
